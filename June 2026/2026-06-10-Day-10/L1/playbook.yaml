## 🔹 Ansible — When Condition
---
  - name: Install tool used based on OS
    hosts: all 
    become: true 

    tasks:
      - name: Install htop 
        yum:
          name: htop 
          state: present 
        when: ansible_facts['os_family'] == "RedHat"