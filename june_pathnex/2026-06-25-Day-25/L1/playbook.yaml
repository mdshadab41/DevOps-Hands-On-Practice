## Ansible — Modify Existing Line

- name: Update SSH config for Nephronix 
  hosts: all 
  become: true 

  tasks:
    - name: Disable root login 
      lineinfile:
        path: /etc.ssh/sshd_config
        regexp: "^PermitRootLogin"
        line: "PermitRootLogin no"
        